const Tag = ({ children }) => {
  return (
    <div className="border border-mediumGray rounded-full px-4 py-0.5 text-xs font-semibold w-fit h-fit">
      {children}
    </div>
  );
};

export default Tag;
